# Cloudflare Pages equivalent of vercel.json's cache rule.
# Vite fingerprints every file in /assets, so they are safe to cache forever.
/assets/*
  Cache-Control: public, max-age=31536000, immutable
