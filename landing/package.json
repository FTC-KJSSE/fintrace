{
  "name": "fintrace-landing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static landing page for FinTrace — built on the same Three.js stack as the application itself.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.169.0",
    "three-globe": "^2.32.1"
  },
  "devDependencies": {
    "vite": "^5.4.8"
  }
}
